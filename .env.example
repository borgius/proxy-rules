# Proxy Rules - Environment Template
# Copy this to .env and fill in values as needed.
# Plugin rules can read these via process.env.

# NODE_ENV=development

# Optional: override default config directory
# PROXY_RULES_CONFIG=~/.proxy-rules

# Optional: secrets your rule plugins may need (referenced as process.env.MY_API_KEY)
# MY_API_KEY=
